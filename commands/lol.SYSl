lang-imp Discord;

from Discord enter 'all';

exe(mess, args);
if{
    user.Discord.role("Admin").then(mess.('Sorry, that might be the wrong emoji...').send);
    } else {
        mess('You cant send this command because you dont have the right permissions.').reply;
