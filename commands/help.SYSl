lang-imp Discord;

from Discord enter 'all';

exe(mess, args);
mess('Public commands: !help, !info, !ched, !invite, !avatar').send;
mess('Admin-only commands: !ping, !lol, !clear {number of messages}').send;
mess.rr.give('764475201668251648').asEmu;

if(i.nick == ' '){
  nick.i('SYSbot', "bc it's my name")
}
